## 環境構築（✅ 完了 → 2026-04-17）

### 背景・ゴール
新しいマシンに開発環境を整える。Python・Node.js・Git の3つが必要。

### 試したこと・詰まったこと
- `python --version` の出力が `Python v24.13.0` と表示されて混乱 → 実は Node.js の出力が混在していた
- WindowsApps の python.exe はMicrosoft Storeへの誘導スタブで、Python 本体ではなかった
- winget で Python 3.13.13 をインストールして解決

### 結果
✅ Python 3.13.13（新規インストール）
✅ Node.js v24.13.0（導入済み）
✅ Git 2.52.0（導入済み）

### 気づき・次に活かせること
- Windows の `python` コマンドはストアスタブの可能性がある。`where python` で実体を確認すること
- インストール後はターミナルを再起動しないと PATH が更新されない

---

## Next.js セットアップ（✅ 完了 → 2026-04-17）

### 背景・ゴール
「Antigravity」フォルダに Next.js 環境を作る。

### 試したこと・詰まったこと
- `create-next-app .` がフォルダ名「Antigravity」の大文字制約でエラー
  → npm のパッケージ名に大文字は使えない仕様
  → 手動セットアップ（package.json を直書き → npm install）で解決
- 最初に入った Next.js 15.3.1 に重大な脆弱性（CVE-2025-66478）があった
  → `npm install next@latest` で 16.2.4 に上げて脆弱性ゼロに

### 結果
✅ Next.js 16.2.4 + TypeScript + Tailwind CSS v4 + ESLint
✅ App Router 構成（`src/app/`）
✅ tsconfig.json・postcss.config.mjs・eslint.config.mjs を手動作成

### 気づき・次に活かせること
- `create-next-app` はフォルダ名に大文字があると失敗する。手動セットアップのほうが柔軟
- Tailwind CSS v4 は `tailwind.config.ts` 不要。`globals.css` に `@import "tailwindcss"` 1行だけでOK
- `src/**/versions/**` を tsconfig.json の exclude に追加しないと、バックアップファイルもビルド対象になってエラーになる

---

## ぷよぷよアプリ作成（✅ 完了 → 2026-04-17）

### 背景・ゴール
10代女性向けのおもしろかわいいUI/UXのぷよぷよアプリをNext.js上に作る。

### 実装内容
- `src/lib/puyoLogic.ts`：ゲームロジック（純粋関数）。BFS連結判定・重力・スコア計算
- `src/components/PuyoGame.tsx`：メインコンポーネント。状態管理・ゲームループ
- `src/components/PuyoCell.tsx`：1セルの描画（アニメーション付き）
- `src/components/ScoreBoard.tsx`・`NextPuyo.tsx`：UI部品

### UIデザイン方針（10代女子トレンド）
- ホログラム風グラデーションタイトル（`holographic` カスタムクラス）
- グラスモーフィズム盤面（`rgba(255,255,255,0.15)` + backdrop-blur）
- ぷよを絵文字で表現（🌸💜💙⭐🍀）
- 消滅時に✨スパークルエフェクト
- 2連鎖以上でチェーンメッセージポップアップ
- Nunito フォント（Google Fonts）

### 結果
✅ ゲームロジック（落下・回転・連鎖・スコア計算）
✅ PCキーボード + スマホタッチボタン両対応
✅ ハイスコアの localStorage 保存
✅ スタート画面・ゲームオーバー画面

---

## バグ修正：React Strict Mode 対応（✅ 完了 → 2026-04-17）

### 背景・ゴール
「ぷよが自動で落ちてこない」「着地後に次のぷよが動かないことがある」という2つの不具合。

### 原因の特定

**バグ1（Strict Mode の二重実行）**
`setInterval` のコールバック内で `setCurrent` の updater 関数を使い、その**中から** `lockPair`（= `setGrid`・`setPhase` を呼ぶ関数）を呼んでいた。
Next.js の開発モードは React Strict Mode が有効で、updater 関数を意図的に2回実行する。
→ `lockPair` が2回呼ばれ、スポーン直後に即座にロックされて「降りてこない」状態になっていた。

`hardDrop` も同じ構造で同じバグがあった。

**バグ2（React のバッチング問題）**
`lockPair` → `runChainCheck`（消えるものなし）→ `setPhase("falling")` が**同一バッチ内**で処理されると、`phase` が「falling → falling」（変化なし）と認識される。
→ 自動落下の `useEffect` が再起動しないことがあり、次のぷよが動かない場合があった。

### 解決策

**バグ1の修正（v3.0）**
`currentRef`・`phaseRef` を導入し、interval と hardDrop を updater 関数を使わない形に変更。`lockPair` を updater の外から直接呼ぶようにした。

**バグ2の修正（fallingKey）**
`fallingKey`（数値カウンター）を state として追加。falling に戻るたびに +1 することで、`phase` が変化しなくても `useEffect` が確実に再起動するようにした。

### 教訓（次の開発に活かす）

- **React の updater 関数（`setState(prev => ...)` の中）では副作用（他の state 変更）を起こさない**
  → Strict Mode で2回実行されてバグになる
- **同一同期処理内で state を A→B→A と変化させても useEffect は再起動しない**
  → React がバッチングして「変化なし」と判断するため
  → カウンター（key）パターンで強制再起動する
- **`setInterval` + React の組み合わせは `useRef` で最新値を持つのが基本**
  → `grid`・`current`・`phase` などをクロージャキャプチャすると古い値を参照する

### 修正の流れ（バージョン履歴）
- v1.0：初版。上記バグをすべて含む
- v2.0：`gridRef`・`nextRef`・`highScoreRef` 導入。クロージャキャプチャ問題を修正
- v3.0：`currentRef`・`phaseRef` 導入。updater 内の副作用問題（Strict Mode バグ）を修正
- v3.0+：`fallingKey` 追加。バッチング問題による useEffect 未起動を修正

---

## GitHub + Vercel デプロイ（✅ 完了 → 2026-04-17）

### 背景・ゴール
作ったゲームを公開する。GitHub にコードを上げて Vercel でホスティング。

### 手順

1. `git init` → `git add .` → `git commit`
2. `gh repo create antigravity --public --source=. --remote=origin --push`
   → GitHub リポジトリ自動作成＆プッシュ（gh CLI 使用）
3. `npx vercel login`（ブラウザで認証）
4. `npx vercel --yes --name antigravity`

### 詰まったこと
- `vercel --yes` だけだとフォルダ名「Antigravity」の大文字でエラー → `--name antigravity` で解決
- GitHub との自動連携（プッシュ→自動デプロイ）は Vercel 管理画面の Authentication でLogin Connectionを追加する必要がある

### 結果
✅ GitHub：https://github.com/mimi-ai-backoffice/antigravity
✅ 公開URL：https://antigravity-five-opal.vercel.app

### 次回デプロイ方法
GitHub との自動連携が未設定のため、手動でデプロイする場合：
```
cd "C:/Users/mimim/OneDrive/Desktop/Claude Code/Antigravity"
git add . && git commit -m "変更内容"
git push
npx vercel --prod
```

自動デプロイを有効にするには：
vercel.com → Settings → Authentication → GitHub を Login Connection として追加
